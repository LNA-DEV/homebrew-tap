# LNA-DEV Homebrew Tap

Custom Homebrew formulae for LNA-DEV projects.

## Installation

Add this tap to your Homebrew:

```bash
brew tap LNA-DEV/homebrew-tap
```

## Adding New Formulae

Place new formula files (`.rb`) in the root of this repository. Homebrew will automatically discover them when users tap this repository.
